# HOME content, photography and audience motion

User-authorized scoped existing-product revision (image-to-code B), 2026-09-10.
No agents. Preserve seven-stage topology, support destinations, existing hover/focus
card expansion, mobile stacking, horizontal expertise choreography and reduced-motion
flow. Unrelated routes/hero/travel stages are outside this request.

## Evidence and authority

HOME reads home-target.ts; headings were localized but five itinerary records,
three camp records, both quoted passages and six camp background IDs were not.
Support only translates its arrow 5px; there is no resting button animation.
Source: references/adelva/sources/{PRODUCT.md,home-copy.md,content-taxonomy.md},
shared adelva-navigation.ts. Reuse shared challenges/serviceDomains to prevent drift.
Aman and The Spirit of Aman are photographic mood references, not page-layout
references; neither their copy nor assets will ship. YouTube text retrieval exposed
only the title, not video frames. Eleven separately generated fictional photographs.
No full-page mock required: existing composition and user-specified photo direction
resolve this scope. Legacy external White Desert fidelity remains a separate gate;
its replaced copy/photos are intentional deviations, never new self-proven fidelity.

## Geometry and typography

Support baseline at 1440: equal 576px links, x=93.59/770.39; gap=100.8px.
Keep these grid widths within 1px, add an inset translucent surface with no layout
shift. Desktop two columns, under 600px stacked, min touch height 44px.
Hover/focus: gold border sweep, warm surface reveal, arrow within circular ring,
text translates 4px. CSS transform/opacity transitions 240ms strong ease-out;
decorative surface settles 650ms. Rest: one slow 4.5s light sweep on viewport entry
(stagger 250ms), finishes rather than endlessly distracting; reduced motion static.
No JS pointer tracking, no new dependencies, no changing click target bounds.

Challenge field keeps existing 765px desktop / 717px mobile card height.
Five exact Japanese headings and source descriptions, numbered 01–05, no prices.
Desktop active body 50vw minus inset; inactive titles visible in vertical rails.
Japanese sans, 28–40px heading / 16–18px body, line-height 1.5/1.85.
At 768 use smaller insets; 390 all five complete bodies visible and no rails.
Photos object-fit cover, center focal, decorative empty alt, lazy next/image.

Expertise keeps three 4:3 desktop / existing portrait mobile cards and six images
(two intro, three domain, one final). Japanese titles 24–40px with natural wrapping,
body >=16px, line-height 1.85; strengthen scrims for all photo/text overlays.
Use existing implemented /challenges#support-* domain anchors while dedicated
/services routes remain pending. This avoids creating new unrequested pages or 404s.
Purpose and founder copy become source-derived Japanese, not a third-party quote.
Preserve existing founder signature/name; no new credentials or claims.

## Acceptance

1440x900, 768x1024, 390x844: no horizontal overflow, clipping, hidden Japanese
headings or failed media; five challenge destinations and three domain anchors
resolve. Two audiences stay equal priority and keyboard-operable. Check resting,
hover, focus, active-card and reduced-motion states; screenshot final sections.
Format, lint, typecheck, unit, Playwright, axe, visual and build run and report actual
results. Legacy reference mismatches are reported distinctly from scoped checks.
