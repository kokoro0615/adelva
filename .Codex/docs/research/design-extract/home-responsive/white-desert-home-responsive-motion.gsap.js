// GSAP presets — generated by designlang (motionlang)
// Source: https://white-desert.com/
// 2026-08-30T11:07:02.736Z
//
//   import gsap from 'gsap';
//   import { registerEases, reveals } from './white-desert.com-motion.gsap.js';
//   registerEases(gsap);            // one-time: registers CustomEases
//   reveals.slideUp('.card');

/** CustomEase path data extracted from the live page, keyed by family. */
export const eases = {
  easeIn: 'M0,0 C0.5,1 0.89,1 1,1', // 127× on page
  custom2: 'M0,0 C0.4,0 0.2,1 1,1', // 5× on page
  custom3: 'M0,0 C0.76,0 0.24,1 1,1', // 4× on page
  easeInOut: 'M0,0 C0.25,0.1 0.25,1 1,1', // 1× on page
  easeOut: 'M0,0 C0.16,1 0.3,1 1,1', // 1× on page
};

/** Duration presets (seconds), extracted from the live page. */
export const durations = {
  md: 0.3,
  lg: 0.45,
  xl: 1,
};

/** Register every extracted curve as a named GSAP CustomEase. Call once. */
export function registerEases(gsap) {
  const CustomEase = gsap.plugins?.CustomEase || (typeof window !== 'undefined' && window.CustomEase);
  if (!CustomEase) {
    console.warn('[motion.gsap] CustomEase plugin not registered — call gsap.registerPlugin(CustomEase) first.');
    return;
  }
  for (const [name, path] of Object.entries(eases)) CustomEase.create(name, path);
}

/** gsap.from() reveal helpers. Pass any GSAP target + optional vars override. */
export const reveals = {
  fadeIn: (target, vars = {}) => gsap.from(target, { opacity: 0, duration: 1, ease: 'easeIn', ...vars }),
  slideUp: (target, vars = {}) => gsap.from(target, { opacity: 0, y: 24, duration: 1, ease: 'easeIn', ...vars }),
  slideDown: (target, vars = {}) => gsap.from(target, { opacity: 0, y: -24, duration: 1, ease: 'easeIn', ...vars }),
  scaleIn: (target, vars = {}) => gsap.from(target, { opacity: 0, scale: 0.96, duration: 1, ease: 'easeIn', ...vars }),
  pop: (target, vars = {}) => gsap.from(target, { scale: 0.9, duration: 1, ease: 'easeIn', ...vars }),
  stagger: (targets, vars = {}) => gsap.from(targets, { opacity: 0, y: 24, duration: 1, ease: 'easeIn', stagger: 0.200, ...vars }), // stagger observed at runtime
};

/** Site uses scroll-/view-timeline. Batch-reveals elements as they enter. */
/** Requires: gsap.registerPlugin(ScrollTrigger). */
export function revealOnScroll(targets, vars = {}) {
  return gsap.utils.toArray(targets).map((el) =>
    gsap.from(el, {
      opacity: 0, y: 24, duration: 1, ease: 'easeIn', ...vars,
      scrollTrigger: { trigger: el, start: 'top 85%', toggleActions: 'play none none reverse' },
    }));
}

export default { eases, durations, registerEases, reveals };
