# Owners mobile correction

ユーザー指定比較47:30は390×6703、本文14/24、見出し28/40、Hero390×844。旧67:3は725幅の全体ボードを390/725倍し390×1167.31にした結果、本文7.53px・工程5.92px・Hero182.36pxになっていた。写真や字体の差分以前に、画像座標とCSS相当サイズを混同したことが原因。前回の原寸PNG一致検証はモバイルUIの可読性を検証していなかった。

修正対象は67:3を維持。幅390、左右24、本文14/24、主要リスト16以上、見出し28/40、Hero見出し32/46、補助ラベル11以上、CTA52、ヘッダー操作44以上。全体を等比縮小しない。セクションはAuto Layoutで内容に合わせ縦方向に伸ばす。章構成・コピーは承認済みOwners Desktopを保持し、47:30のUI寸法・ヘッダー・共通CTAを再利用。

写真は390×844用の縦構図をimagegenで新規生成。回廊・ダイニングは同じFigmaにある生成済み単体写真を再利用。全体UIはFigmaネイティブ文字で構成し、修正版PNGをFigmaから書き出す。旧ボードを縮小再利用しない。

## 完了結果

67:3を390×6974へ修正。8内容章と2写真区画、114テキスト。見出し28/40・Hero32/46、本文14/24、支援詳細16/26、主要行18、工程18/28、補助11–13、SCROLL/breadcrumbのみ10。ヘッダーCTA44、本文CTA52。比較47:30と同じヘッダー・ロゴ・CTAを使用。原画像の色・ホテル世界観を保った専用Heroはimagegen生成853×1844。

最終PNG `../long-page-mock-mobile-390.png` はFigma書き出しの実寸390×6974。画面プレビュー86:16は390×844・縦スクロール設定、複製内容は本体と完全一致。写真は架空空間の生成素材、UIテキストはネイティブ。新規Heroの正確なプロンプトはmobile-390-audit.json。全体画像の再生成でテキスト寸法を任せず、Figmaで寸法を確定してPNGにした。

各内容章と最終Heroを原寸で目視。テキスト枠の重なり0、親からのはみ出し0、欠落フォント0、主要コピー照合の欠落0。数値検証はmobile-390-audit.json。Figmaプレイヤーの実操作は未試験。フォーム・メニュー遷移の実装は本静止モックの対象外。

旧725幅ボード66:54はアーカイブ表示へ改名。Desktop、GM参照47:30、原PNGは保持。旧縮小版のFigma子ノードを置換し、途中・重複スクリーンショットを整理。今回Webコード変更なし。
